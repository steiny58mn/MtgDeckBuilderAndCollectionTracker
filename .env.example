# Turso Database Configuration
# These can be configured in Cloudflare Pages / Workers Variables & Secrets,
# or your container/hosting environment variables.
# Never store raw database tokens or URLs in plaintext in source code.
TURSO_DATABASE_URL=libsql://your-database-name.turso.io
TURSO_AUTH_TOKEN=your-turso-auth-token
